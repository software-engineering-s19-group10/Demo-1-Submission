trainAndDetect.sh takes the images in the dataset, creates feature vectors, and serializes / saves the vectors in faces.db.  Must have OpenCV library in directory and shell script must be edited based on current workspace before being ran.

